/**
 * Nothing to set up on the server side — the bot auto-generates per-user
 * custodial wallets on first /wallet command. This stub exists so
 * `npm run setup` doesn't fail; delete once everyone knows.
 */
console.log("Parasocial: no setup required. Just `npm run dev` and /wallet in Telegram.");
